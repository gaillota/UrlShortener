{% extends 'AGUserBundle::layout.html.twig' %}

{% trans_default_domain 'FOSUserBundle' %}

{% block breadcrumbs %}
	{{ parent() }}
	<li><a href="{{ path('fos_user_profile_show') }}">Mon compte</a></li>
	<li class="active">Editer mes informations</li>
{% endblock %}

{% block ag_user_content  %}
	<div class="row">
		<div class="col-sm-12">
			<h2>{{ fa('edit') }} Editer mes informations</h2>
			<hr/>
		</div>
	</div>
    <div class="row">
        <div class="col-md-12">
	        <form action="{{ path('fos_user_profile_edit') }}" {{ form_enctype(form) }} method="POST" class="fos_user_profile_edit">
		        <div class="form-group">
			        {{ form_label(form.username) }} :
			        {{ form_errors(form.username) }}
			        {{ form_widget(form.username, {'attr' : {'class': 'form-control'}}) }}
		        </div>
		        <div class="form-group">
			        {{ form_label(form.email) }} :
			        {{ form_errors(form.email) }}
			        {{ form_widget(form.email, {'attr' : {'class': 'form-control'}}) }}
		        </div>
		        <div class="form-group">
			        {{ form_label(form.current_password) }} :
			        {{ form_errors(form.current_password) }}
			        {{ form_widget(form.current_password, {'attr' : {'class': 'form-control'}}) }}
		        </div>
		        {{ form_rest(form) }}
		        <div>
			        <input class="btn btn-default" type="submit" value="{{ 'profile.edit.submit'|trans }}" />
		        </div>
	        </form>
        </div>
    </div>
{% endblock %}
