<div class="row">
    <div class="col-md-6">
        <h3>{{ fa('list') }} Détails</h3>
        <hr>
        <h4>{{ fa('globe') }} URL</h4>
        <p><a href="{{ link.url }}" target="_blank">{{ link.url }}</a></p>
        <h4>{{ fa('link') }} Lien</h4>
        <p><a href="{{ url('ag_shortener_redirect', { token: link.token }) }}" target="_blank">{{ url('ag_shortener_redirect', { token: link.token }) }}</a></p>
        <h4>{{ fa('hand-o-up') }} Cliques</h4>
        <p>{{ link.clicks|length }} clique(s)</p>
    </div>
    <div class="col-md-6">
        <h3>{{ fa('qrcode') }} QR Code</h3>
        <hr>
        <img src="{{ qrcode_data_uri(url('ag_shortener_redirect', {token: link.token }), size=200) }}" alt="QR" class="img-responsive">
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <div id="chart_div" style="width: 100%; height: 400px;"></div>
    </div>
</div>